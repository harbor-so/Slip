{
  "name": "harbor-cloudflare-sandbox-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deployable Cloudflare Worker that exposes Harbor's sandbox control API over HTTP, backed by @cloudflare/sandbox Durable Objects + Containers.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.12.5"
  },
  "devDependencies": {
    "wrangler": "^4.123.0",
    "@cloudflare/workers-types": "^5.20260813.1"
  }
}
