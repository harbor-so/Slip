apiVersion: v1
kind: Service
metadata:
  name: harbor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http
