apiVersion: v1
kind: Service
metadata:
  name: harbor-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 8788
      targetPort: http
