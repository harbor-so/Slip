/**
 * The reference demo's Scene 1 — the "New goal" cold open — recreated with
 * Harbor's names. Matches harbor-app/apps/demo `/demo`.
 */

import { DemoGoal } from "@web/chat/DemoGoal.js";

export const dynamic = "force-dynamic";

export default function DemoPage() {
	return <DemoGoal />;
}
